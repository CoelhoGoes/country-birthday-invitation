# Textura de fundo

Coloque aqui a imagem de textura de papel kraft/bege tileável usada como
fundo da página. Nome esperado pelo código:

- `kraft-paper.jpg` (ou `.png`) — textura tileável, tons de bege/kraft.

Enquanto o arquivo não existir, a página usa a cor sólida `kraft` (definida
em `tailwind.config.ts`) como fallback — nada quebra visualmente.
